# Copy to .env to enable the real LLM intent parser. Optional — the app works without it.
ANTHROPIC_API_KEY=
# Model used only to map a question to a KPI + parameters (never to write SQL).
INTENT_MODEL=claude-haiku-4-5-20251001
